"use client";
import{useEffect,useState}from"react";import{useParams}from"next/navigation";import{Network}from"lucide-react";
const API=process.env.NEXT_PUBLIC_API_URL??"http://127.0.0.1:8000";type Data={work:{canonical_title:string};nodes:{entity_id:string;name:string;summary:string}[];edges:{source_id:string;target_id:string;source_name:string;target_name:string;relation:string;note:string}[]};
export default function Relationships(){const{workId}=useParams<{workId:string}>();const[data,setData]=useState<Data|null>(null);useEffect(()=>{fetch(`${API}/library/works/${workId}/relationships`).then(r=>r.json()).then(setData)},[workId]);if(!data)return <main className="loading">正在载入人物关系…</main>;return <main className="libraryPage"><section className="pageIntro compact"><p className="eyebrow">RELATIONSHIPS</p><h1>《{data.work.canonical_title}》人物关系</h1><p>关系保留方向、类型和解释。</p></section><div className="relationshipList">{data.edges.map((edge,index)=><article key={`${edge.source_id}-${edge.target_id}-${index}`}><Network/><div><strong>{edge.source_name}</strong><span>{edge.relation}</span><strong>{edge.target_name}</strong><p>{edge.note}</p></div></article>)}</div><h2 className="sectionHeading">人物档案</h2><div className="cardGrid">{data.nodes.map(node=><article className="knowledgeCard" key={node.entity_id}><small>{node.entity_id}</small><h3>{node.name}</h3><p>{node.summary}</p></article>)}</div></main>}