"use client";
import{useEffect,useState}from"react";import{useParams}from"next/navigation";
const API=process.env.NEXT_PUBLIC_API_URL??"http://127.0.0.1:8000";type Data={work:{canonical_title:string};items:{entity_id:string;entity_type:string;name:string;summary:string;attributes:Record<string,string|number>}[]};
export default function Timeline(){const{workId}=useParams<{workId:string}>();const[data,setData]=useState<Data|null>(null);useEffect(()=>{fetch(`${API}/library/works/${workId}/timeline`).then(r=>r.json()).then(setData)},[workId]);if(!data)return <main className="loading">正在载入时间线…</main>;return <main className="libraryPage"><section className="pageIntro compact"><p className="eyebrow">MULTI-TRACK TIMELINE</p><h1>《{data.work.canonical_title}》时间线</h1><p>当前 v0 以文字节点展示，后续可升级为交互时间轴。</p></section><div className="timelineList">{data.items.map((item,index)=><article key={item.entity_id}><span className="timelineIndex">{String(index+1).padStart(2,"0")}</span><div><small>{String(item.attributes.track??item.entity_type).toUpperCase()}</small><h2>{item.name}</h2><p>{item.summary}</p></div></article>)}</div></main>}