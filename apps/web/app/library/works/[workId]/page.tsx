"use client";
import Link from"next/link";import{useEffect,useState}from"react";import{useParams}from"next/navigation";import{ArrowRight,Clock3,Network}from"lucide-react";
const API=process.env.NEXT_PUBLIC_API_URL??"http://127.0.0.1:8000";type Detail={work:{work_id:string;canonical_title:string;creators:string[];release_year:number};dossier_version:string;entity_counts:Record<string,number>};
export default function WorkDetail(){const{workId}=useParams<{workId:string}>();const[item,setItem]=useState<Detail|null>(null);useEffect(()=>{fetch(`${API}/library/works/${workId}`).then(r=>r.json()).then(setItem)},[workId]);if(!item)return <main className="loading">正在载入作品…</main>;return <main className="libraryPage"><section className="workHero"><small>WORKDOSSIER {item.dossier_version}</small><h1>《{item.work.canonical_title}》</h1><p>{item.work.creators.join(" · ")} · {item.work.release_year}</p><div className="chips">{Object.entries(item.entity_counts).map(([key,value])=><span key={key}>{key} {value}</span>)}</div></section><div className="detailLinks"><Link href={`/library/works/${workId}/relationships`}><Network/><div><small>VIEW 01</small><h2>人物关系</h2><p>人物、集体行动者及其有方向的关系。</p></div><ArrowRight/></Link><Link href={`/library/works/${workId}/timeline`}><Clock3/><div><small>VIEW 02</small><h2>时间线</h2><p>真相、调查、叙述与裁决节点。</p></div><ArrowRight/></Link></div></main>}