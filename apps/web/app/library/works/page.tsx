"use client";
import Link from "next/link";import{useEffect,useState}from"react";import{ArrowRight,BookOpen}from"lucide-react";
const API=process.env.NEXT_PUBLIC_API_URL??"http://127.0.0.1:8000";type Item={work:{work_id:string;canonical_title:string;release_year:number;creators:string[]};dossier_version:string;entity_count:number};
export default function Works(){const[items,setItems]=useState<Item[]>([]);useEffect(()=>{fetch(`${API}/library/works`).then(r=>r.json()).then(setItems)},[]);return <main className="libraryPage"><section className="pageIntro compact"><p className="eyebrow">WORK COLLECTION</p><h1>作品集</h1><p>每部作品拥有独立数据边界与版本。</p></section><div className="workLinks">{items.map(item=><Link href={`/library/works/${item.work.work_id}`} key={item.work.work_id}><BookOpen/><div><h2>《{item.work.canonical_title}》</h2><p>{item.work.creators.join(" · ")} · {item.work.release_year}</p><small>DOSSIER {item.dossier_version} · {item.entity_count} 个实体</small></div><ArrowRight/></Link>)}</div></main>}